{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    "baseUrl": ".", // 프로젝트 루트를 기준으로 설정
    "paths": {
      "@styles/*": ["src/styles/*"], // @styles alias 설정
      "@components/*": ["src/components/*"] // 추가 alias 설정 (선택 사항)
    }
  }
}
