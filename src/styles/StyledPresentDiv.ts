import styled from '@emotion/styled'

const StyledPresentDiv = styled.div`
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  background-color: green;
  gap: 4px;
  width: 720px;
`

export default StyledPresentDiv
